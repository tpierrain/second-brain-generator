// demo-locale.mjs — which locale this brain was installed with. Root = "en"
// (the default). The fr overlay (templates/fr/) replaces this file with "fr".
// demo.mjs reads it to pick the locale-appropriate canary question.
export const BRAIN_LOCALE = "en";
