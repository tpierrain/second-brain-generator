<!-- ════════════════════════════════════════════════════════════════════════ -->
<!-- STATUS: 🚧 ACTIVE (2026-06-21) — the SHORT remaining-to-ship plan for v3.3.0. -->
<!-- Supersedes the long v3.3.0 cluster (archived/: self-converging-…, code-review- -->
<!-- fixes-, qa-plan-, self-heal-converge-…). Thomas's QA is DONE; it found 2 bugs   -->
<!-- that block ship. Fix both, prove them on the real upgrader rig, then ship.       -->
<!-- ════════════════════════════════════════════════════════════════════════ -->

# v3.3.0 — what's left before we can ship

> ▶️ **NEXT SESSION — START HERE.** This is the **single** active plan. Two ship-blockers remain (A, B).
> 🔴 **A WAS REOPENED — read the ⛔ CRITICAL FINDING under section A FIRST.** Short version: the restart
> nudge was built on `statusLine`, which **Claude Desktop does NOT render** (Thomas confirmed on-screen).
> The fix lives in the **CHAT**. **A1 (loud core report, reaches upgraders), A4 (skill ALWAYS says restart,
> never "nothing to do") and A6 (SessionStart `additionalContext` nudge → agent relays it into the chat) are
> DONE (2026-06-21).** Only **A5 remains** (deliver the updated skill to existing brains — same disease as
> Fix B, likely do it with B) and it is **NOT** needed for the steady-state QA to pass (the A1 core banner
> already reaches upgraders). Prove the fix **in Desktop chat**, not CLI. Rig: `~/legacy-brain` fresh v3.1.0
> → `qa-v33-src.git` @ `0057ac7`.
> TDD, green-only local commits; **no push/merge/tag without Thomas's explicit green light.** Durable state
> lives **here**, not in `memory.md`.

## The decision (Thomas, 2026-06-21)

The manual QA campaign is **finished**. It surfaced **two ship-blocking bugs**, both instances of the
same disease: *an engine improvement is trapped behind an artifact that never reaches existing brains*
(every one of Thomas's brains is an upgrader). **Fix these two, prove them on the real v3.1.0 rig, ship.**
No new feature scope. Durable state lives **here, in the repo** (not in `memory.md` — the oversized
memory index caused the context-loss side effects we are correcting).

Discipline: **TDD baby-steps** (`tdd-discipline`), **green-only local commits**, **deterministic-first**
(ADR 0009), **Mac/Win/Linux parity** (ADR 0015), **artifacts in English**. **Prove on the real rig**
(disk-level ABSENT→PRÉSENT, the link actually appears) — not a staged unit test. No CI; run suites
locally. **No push/merge/tag without Thomas's explicit green light.**

## Tracking

> ⛔ **CRITICAL FINDING (Thomas's Desktop QA, 2026-06-21) — REOPENS A. The `statusLine` does NOT
> render in Claude Desktop's Code tab.** Confirmed by Thomas on his own screen: there is no status bar at
> all. So **A2's entire channel (the persistent statusLine nudge) is invisible on Desktop** — the surface
> that matters. The `isRestartPending`/gap-detection/flag code is *correct* and proven at CLI level
> (`dfff2be`, `0057ac7`) but lands on a dead channel for Desktop users. **DO NOT count A2 as the fix.**
>
> **The ONLY Desktop-visible channel is the CHAT** (the agent's text). The chat text comes from two
> sources: (1) the **core report** printed by `update-engine.mjs` — which the agent relays, and which
> **DOES reach all upgraders** (`replace`/replaceScripts) and is already loud (A1, `2f6c3ca`); and (2) the
> **`update-engine` SKILL.md** instructions the agent follows — which **does NOT reach upgraders**
> (install-if-absent) and **still says "nothing to do on your side"**, actively contradicting the restart
> banner. Thomas's first update ran the OLD core (silent) **and** the OLD skill ("rien à faire") → double
> silence.
>
> **REMAINING WORK for A — make the AGENT ALWAYS say "restart" IN THE CHAT:**
> - [x] **A4 — `update-engine/SKILL.md`: (a) ALWAYS instruct the agent to tell the user to restart Claude,
>   in their language, whenever the engine changed; (b) DELETE every "nothing to do / rien à faire" framing
>   that undercuts it.** _(2026-06-21 — Step 3 rewritten: a 🛑 MANDATORY block with a ready-to-use loud
>   `## 🔄 ⚠️ RESTART CLAUDE NOW ⚠️ 🔄` banner; fires on EVERY swap (not only new capabilities); the genuine
>   no-op is the only skip; explicitly bans "nothing to do / rien à faire / you're all set".)_
> - [ ] **A5 — DELIVER the updated `update-engine` SKILL.md to EXISTING brains.** Today engine skills are
>   install-if-absent (ADR 0025) → the old skill persists on upgraders. Options: a *replace-if-present* carve-out
>   for engine-OWNED skills (update-engine is pure engine machinery, not user-customized), or the Fix-B managed
>   block applied to the skill. **This is the same root disease as Fix B** (engine improvements to user-space
>   files don't reach upgraders) — consider doing A5 and B together. _(NOTE: the steady-state QA already passes
>   WITHOUT A5 — the **core report banner (A1) reaches all upgraders via `replace`** and the agent relays it; A5
>   only makes the SKILL's own amplification reach upgraders too.)_
> - [x] **A6 — irreducible boundary, document + minimize + the Desktop-visible SessionStart nudge**: the VERY
>   FIRST update from a pre-3.3 brain runs the OLD core + OLD skill = silent in the chat, unavoidably (the update
>   is what installs the loud behavior). From the 2nd update / next session on, it is loud. _(2026-06-21 — built
>   the SessionStart `additionalContext` chat nudge: `session-self-heal.mjs` now emits via
>   `hookSpecificOutput.additionalContext` (the agent relays it INTO THE CHAT = the ONLY Desktop-visible channel),
>   NOT only `systemMessage` (Desktop-dropped). New pure `buildSelfHealHookOutput` + RED→green test. So once the
>   self-heal hook has converged onto an upgrader, every later session that still has a pending restart tells the
>   user loudly in the chat.)_
> - [x] **A7 — THE "MESSAGE IN A BOTTLE" (Thomas's idea, 2026-06-21): make the FIRST pre-3.3 update loud in the
>   chat WITHOUT a second update and WITHOUT the user remembering to restart.** The OLD orchestrator is frozen, but
>   it ALWAYS runs `npm install` in `rag/` with `stdio:"inherit"`, and npm ALWAYS runs the `postinstall` of the
>   package.json THEN ON DISK = the NEW one (replace regime, copied BEFORE install). So a NEW `postinstall` runs
>   UNDER the OLD orchestrator, deterministically, on the very first update, and its stdout flows into the chat the
>   agent reports from. _(2026-06-21 — new `rag/postinstall-restart-notice.mjs` (pure `shouldNudgeRestart` +
>   `restartNoticeBanner`, RED→green) + `postinstall` wrapper in `rag/package.json` + exact `replace` glob so the
>   OLD copy-select carries it. Fires ONLY during an update via a deterministic update-vs-install signal: at
>   postinstall time the new `rag/package.json` version is on disk but the manifest still records the OLD rag
>   version (it is rewritten AFTER npm install) → `recorded != package` ⇒ update mid-flight ⇒ loud agent-directed
>   banner; fresh install ⇒ versions equal ⇒ silent. PROVEN end-to-end with the OLD v3.1.0 libs: they copy the .mjs
>   (not its .test.mjs) → npm runs it → banner. Fail-soft: a postinstall hiccup never aborts `npm install`.)_
> - [x] **Re-proven on the rig, IN DESKTOP (chat), not CLI** _(2026-06-21 — Thomas's screen)_: a SINGLE
>   `/update-engine` on a fresh v3.1.0 `~/legacy-brain` printed, in the CHAT, "🚨🚨🚨 ACTION REQUISE — REDÉMARRE
>   CLAUDE MAINTENANT" (localized to FR by the agent from the English agent-directed postinstall banner). The A7
>   message-in-a-bottle works on the very first pre-3.3 update — no second update, no user remembering to restart.

- [x] **A — The user is RELIABLY told to restart Claude when a restart is required** _(✅ DONE + PROVEN IN DESKTOP 2026-06-21. A1 core banner + A4 skill always-restart + A6 SessionStart additionalContext + A7 npm-postinstall message-in-a-bottle. The A7 bottle proved on the real v3.1.0 rig: a SINGLE first update shows the loud localized "REDÉMARRE CLAUDE MAINTENANT" in the chat. A5 — deliver the updated skill to upgraders — left as a non-gating reinforcement to fold into Fix B; the core banner + bottle already reach upgraders.)_
  - [x] A1 — `/update-engine`'s **chat report** (Desktop DOES render chat) ends with a loud, unmissable
    "⚠️ restart Claude now" **whenever it placed engine files that need a restart**. _(steady-state + 2nd jump on.)_
    _(2026-06-21 · `2f6c3ca` — `formatReport` now appends the loud banner on any `copied>0 || regenerated`,
    not only on new capabilities; genuine no-op stays silent. TDD red→green + true-no-op boundary test.)_
  - [x] A2 — the SessionStart self-heal nudge surfaces via **`status-line.mjs`** (the Desktop channel),
    **persistent**, NOT via `systemMessage` (which Desktop drops, `session-status.mjs:7`). Deterministic
    trigger: on-disk engine state is ahead of what this session loaded → show "⚠️ restart needed"; clear once
    a fresh session has loaded it.
    _(2026-06-21 · `bc71a27` — new pure `restart-nudge.mjs` + gitignored `.cache/restart-needed` flag: self-heal
    writes it on a gap, clears it on a converged start; `status-line.mjs` leads its line with the nudge. Both
    files reach upgraders (`scripts/lib/**` replace + `status-line.mjs` replaceScripts).)_
    _(2026-06-21 · `dfff2be` — **HARDENED after Thomas's rig QA found the flag-only nudge NEVER fired on the
    silent first update**: the OLD orchestrator neither converges nor writes the flag, so the user wasn't told
    to restart, kept the stale session, and local-mirror stayed dead. Fix: `statusLine` re-runs continuously and
    reads `status-line.mjs` fresh from disk, so the new version runs even in the STALE session the instant the
    update delivers it. It now ORs two signals (`isRestartPending`): an on-disk GAP (delivered-but-not-installed
    → fires in the SAME silent-update session, no flag/restart needed) OR the flag (converged-but-not-loaded).
    The new core also arms the flag after any restart-needing update. Proven on an isolated tgz rig: cycle-1
    silent → new status-line nudges in-session; converged brain → clean, no false positive.)_
  - [x] A3 — honest caveat (F-B7c): the **very first** `/update-engine` from a pre-3.3 brain runs the OLD
    skill → its report is silent; we cannot rewrite already-installed code retroactively. Mitigation: after
    one restart the new report + status-line take over and become loud. Document this one-restart blind spot.
    _(2026-06-21 · `update-engine/SKILL.md` — added the "Honest one-restart blind spot" note covering the A1
    report banner + A2 status-line nudge: first update delivers the new code, one restart and every later
    update is loud.)_
  - [x] _Proved on the real rig (2026-06-21, `~/legacy-brain` reset from `legacy-brain.tgz` = genuine v3.1.0):_
    - _**A3 blind spot demonstrated**: cycle-1 `/update-engine` (OLD v3.1.0 orchestrator) swapped 228 files yet
      its report was SILENT on restart — exactly the disease._
    - _**A1 proved**: cycle-2 `/update-engine` (NEW orchestrator) on a steady-state swap shows the loud
      "⚠️ ACTION NEEDED — … RESTART … Until you restart, your brain keeps using the old engine."_
    - _**A2 proved**: a fresh self-heal detected the local-mirror skill+MCP gap → wrote `.cache/restart-needed`
      → the NEW `status-line.mjs` LED its line with "⚠️ RESTART Claude to finish the engine update"; once the
      background reconcile converged, a fresh self-heal CLEARED the flag → status-line clean (RAG 8/8)._
    - _CLI-deterministic proof of the exact string Desktop renders on `statusLine`; the final visual
      confirmation in the Desktop UI is Thomas's one-look check._

- [ ] **B — Engine improvements to the constitution REACH existing brains (starting with the dual-link citations)** _(🔴 F-B7e — the DUAL-LINK CITATION bug Thomas saw is now FIXED via two complementary paths; the broader managed-block class remains)_

  > ✅ **THE CITATION SHIP-BLOCKER IS ADDRESSED (2026-06-21).** Thomas's steer: don't dramatize the
  > SACRED-constitution blocker — the engine-owned belt sidesteps it. Two commits:
  > - **`e85caa5`** — the EN constitution template had **NO** citation directive at all (the FR one carried it
  >   since `5d61ce6`); even a FRESH English brain was missing it. Mirrored the directive into
  >   `CLAUDE.md.template` + a **parity test** (`scripts/lib/constitution-mirror-citations.test.mjs`) locking BOTH
  >   templates. _(fresh-install gap closed.)_
  > - **`ac24e06`** — **B5 belt**: `formatSearchCitations` (rag/src, `replace` regime) now **prepends the relay
  >   directive** to `search_vault` output whenever a cited note carries a 🔗 source link. Engine-owned → reaches
  >   EVERY brain on the next `/update-engine`, adjacent to the data. This is the **delivery path that does NOT
  >   need the managed block** for the citation bug. _(rag 209 ✔, harness 492 ✔, tsc clean.)_

  - [ ] B1 — wrap the engine-owned instructions of the constitution in a delimited
    **`<!-- ENGINE:BEGIN --> … <!-- ENGINE:END -->`** managed block. Personal prose stays outside. _(Option 3,
    prior art: Ansible "BEGIN/END MANAGED BLOCK".)_ _(STILL the root-class fix for (2)/(3) below; the citation (1)
    is already delivered via the B5 belt + template parity, so B1 is no longer gating the citation bug.)_
    - **The exact payload a `≤ 3.1` brain is missing** (diff `08b388c`=v3.1.0 → HEAD on `CLAUDE.md.template`):
      - [x] (1) **dual-link mirror citations** (`5d61ce6`) — 🧠 local copy + 🔗 Notion source, "relay them as-is".
        _(the bug Thomas saw — DELIVERED to upgraders via the engine-owned B5 belt `ac24e06`, and to fresh brains
        via the constitution EN+FR parity `e85caa5`. No managed block needed for this one.)_
      - [ ] (2) **the "open a note → default Markdown editor" rewrite** (`8755109`) — replaces the OLD
        "open via `obsidian://` URI" instruction. ⚠️ a `≤ 3.1` brain still tells Claude to open notes via
        `obsidian://` → that IS the dead-link behaviour (F-B4). This rewrite must reach upgraders. _(NOTE: the
        renderer already emits a real-file `file://` link + the "open citation N" opener as the robust channel, so
        a stale constitution degrades gracefully here — candidate to fold into the managed block, not a hard blocker.)_
      - [ ] (3) **the whole "Local mirrors" section** (`d52a59c`→`d92b59d`) — tools table + routing rule for the
        `local-mirror` flagship; a `≤ 3.1` constitution never mentions local-mirror, so Claude lacks the routing
        guidance even though the MCP+skill now converge (F-B7).
    - [ ] ⚠️ **the FR template lags** (`templates/fr/CLAUDE.md.template`): it only received (1) since v3.1.0, NOT
      (2)/(3) — so even a FRESH French brain may be missing the editor rewrite + the local-mirror guidance. Confirm + fix.
      _(Reverse gap also found + fixed: the **EN** template was missing (1) entirely — `e85caa5`.)_
  - [ ] B2 — the engine MAY write the managed **block** inside `CLAUDE.md` even though the file is otherwise
    sacred: **install-if-absent** for the block, **replace-if-present** for its interior; personal prose outside
    is NEVER touched. _(Carve the block out of the `SACRED_FILES` scrub for the block only.)_
  - [ ] B3 — legacy migration = **safe by construction (additive injection)**: a brain with no markers gets the
    block **injected** at a deterministic location; the engine never parses/rewrites the user's existing prose.
    Automatic, deterministic, zero clobber risk. _(No re-render-base surgery, no 3-way merge.)_
  - [ ] B4 — **wire-or-retire the dangling provenance** (`reseedProvenance`, `update-engine.mjs:166`): under the
    managed-block approach there is no 3-way merge → remove/repurpose it so it stops *looking* done. _(The
    "provenance-without-a-consumer" trap that made us circle for 2 days.)_
  - [x] B5 — belt (cheap, engine-owned): keep the relay directive in `search_vault` output too, adjacent to the data.
    _(2026-06-21 · `ac24e06` — `formatSearchCitations` prepends "Relay BOTH 🧠+🔗 links as-is" when a mirror note is
    cited; RED→green, fires only on a present mirror note. THE delivery path for the citation bug to upgraders.)_
  - [ ] B6 — ADR: promote `prospective/engine-managed-file-merge-strategy.md` → an ADR (Crux + prior art);
    amend ADR 0012 in place if it changes the merge-regime mechanics.
  - [ ] _Prove on rig: on `~/legacy-brain`, after `/update-engine` + restart, a question citing a mirror note
    shows **BOTH** 🧠 and 🔗 links — the proof Thomas has never seen._

- [ ] **Ship gate**
  - [x] **Codename DECIDED** (Thomas, 2026-06-21): **"The One Where Updates Finally Reach Already-Installed
    Brains"** _(subtitle: no more "shipped but never landed for you" — `/update-engine` delivers hot + nudges
    restart)_. Use it VERBATIM as the **PR title** and the **release tag title**. Centers v3.3.0 on its real
    hero — engine fixes reaching brains that are ALREADY installed, live (Fix A restart nudge + self-converging
    skill/MCP/hooks/health-note + B5 citation belt). Scope CONFIRMED: citations shipped, managed block (B1/(2)/(3))
    deferred post-ship.
  - [x] **No QA `source` leak** _(Thomas, 2026-06-21)_: the rig repoints a DISPOSABLE brain's manifest
    `source` at the LOCAL bare repo (`/Users/tpierrain/qa-v33-src.git` + branch ref) so `/update-engine`
    pulls the un-pushed work. Guard added so that pointer can NEVER reach the committed launcher manifest
    (which must carry NO `source` — the installer injects the GitHub origin per-brain). Deterministic belt:
    `engine-manifest-integrity.test.mjs` "pins NO source" — proven teeth (RED on an injected QA source).
    _(So no manual pre-ship scrub is needed; the test fails loud if a QA repoint is ever committed.)_
  - [ ] Full suites green (harness + rag + local-mirror) + `tsc` clean.
  - [ ] **Mutation testing** (Stryker) on the new/changed code (managed-block + nudge); surviving mutants =
    harden the tests. _(Objectify test quality, don't assert it — Thomas's root-cause #2.)_
  - [ ] **One real-rig run proves everything at once**: reset `~/legacy-brain` from the tarball → repoint mirror
    → ONE `/update-engine` + restart → (a) dual links appear, (b) restart nudge shows, (c) F-B7/F-B7b skill+MCP
    +health-note still converge. _(Folds in the pending F-B7b "5h" rig re-verify.)_
  - [ ] `/code-review` the branch (`main...HEAD`); fix findings in TDD, green-only.
  - [ ] **Confidential purge**: `rm -rf ~/legacy-brain` after any pass that mirrored a real Notion zone; tarball untouched.
  - [ ] `git tag -d v3.3.0` (disposable tag on the dev repo) before any real release.
  - [ ] **On Thomas's explicit green light**: merge + tag **v3.3.0** + codename + archive this plan.

## Deferred (post-ship — recorded so nothing is silently dropped, NOT gating)

- [ ] F-B4 — the 🧠 local-copy `obsidian://` link is dead in Desktop (the "open citation N" opener already works as the fallback).
- [ ] F-B5 — dense RAG misses a short acronym → hybrid search / reranker (rag Steps 6/7).
- [ ] local-mirror auto-refresh — spike done; Thomas owes the Q5/Q9 go/no-go (`prospective/local-mirror-auto-refresh-spike.md`).
- [ ] Extend the engine-managed model beyond the constitution block (shipped skills, other provided files) — the rest of the root-class fix.

## History (shipped v3.3.0 work — full detail in `archived/`)

P0 + F1 + F2 (citations) + F4 + F5 + F6 + F7/F7-ter (health-check) + F8.2/F8.3 (Obsidian register/health) +
F-B2/F-B3 + F-B7/F-B7b (self-heal converges skill+MCP+hooks+health-note) all shipped green locally on
`v3.3.0-self-converging-brain-and-citations`, NOT pushed. See archived plans:
`v3.3.0-self-converging-brain-and-citations-action.md`, `v3.3.0-code-review-fixes-action.md`,
`v3.3.0-qa-plan-action.md`, `self-heal-converge-mcp-skill-from-pre-3.3-action.md`.
