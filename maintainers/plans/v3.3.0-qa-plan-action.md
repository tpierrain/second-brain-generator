<!-- ════════════════════════════════════════════════════════════════════════ -->
<!-- STATUS: 🚧 IN PROGRESS (started 2026-06-20) — v3.3.0 pre-ship QA campaign.    -->
<!-- ════════════════════════════════════════════════════════════════════════ -->

# v3.3.0 — pre-ship QA plan (split: Claude-automated vs human-only)

## Context

All v3.3.0 features (**F1→F8**) and the **8 `/code-review` findings** are **shipped green, committed,
NOT pushed** on branch `v3.3.0-self-converging-brain-and-citations`. The only thing standing between the
branch and `Ship` (merge + tag + codename + archive) is the **Verification / manual QA** gate of
`v3.3.0-self-converging-brain-and-citations-action.md` (§ "Verification" + "Ship").

This document is the **execution plan for that gate**, split so the work is parallelizable:

- **Part A — 🤖 Claude runs it now** (deterministic, headless, no GUI): the three suites, `tsc`, the
  headless upgrade replay against the pristine tarball, the headless health-check / citation / Obsidian
  builders, the cross-OS `win32`-branch unit checks, the confidential-hygiene grep.
- **Part B — 🧑 Thomas runs it by hand** (real Claude Desktop / real Obsidian GUI / real Notion / OS
  toasts): the things that, by construction, only a human in front of the app can observe.
- **Part C — sign-off**: confidential purge, final `/code-review`, then green light to ship.

Discipline reminders: artifacts in English; **deterministic-first** (ADR 0009); **Mac/Win/Linux parity**
is a hard gate (ADR 0015); no CI → suites run locally; **a red QA item that needs a code fix is fixed in
TDD baby-steps, green-only commit** (it does not get waved through). Push / merge / tag on Thomas's
explicit green light only.

> **How to read the owner tags:** 🤖 = I (Claude) can do it in this repo without a GUI. 🧑 = needs a human
> in front of Claude Desktop / Obsidian / a real Notion workspace, or a second physical OS. 🤝 = I prepare /
> drive it, Thomas confirms the human-visible part.

---

## Tracking

- [x] **A1 — All automated suites + build are green** 🤖 _(2026-06-20)_
- [x] **A2 — F1 headless upgrade replay: v3.1.0 → v3.3.0 converges in one pass, idempotent, vault-safe** 🤖 _(2026-06-20)_
- [x] **A3 — F2 citations render with the affordance, both links + copyable path preserved** 🤖 _(2026-06-20)_
- [x] **A4 — F7/F7-bis/F7-ter health-check: headless light+full, never boots a 2nd server, activation gate** 🤖 _(2026-06-20)_
- [x] **A5 — F8.2/F8.3 Obsidian builders: register idempotent + no-clobber + dedup, soft probe never `broken`** 🤖 _(2026-06-20)_
- [x] **A6 — #8 notify escaping holds for quote/backtick/`$` on darwin+win32** 🤖 _(2026-06-20)_
- [x] **A7 — Cross-OS parity: every `win32` branch exercised from this Mac** 🤖 _(2026-06-20)_
- [x] **A8 — Confidential hygiene: all client tokens scrubbed from the repo; grep clean (F6)** 🤖 _(2026-06-20)_
- [ ] **B1 — F1 real single-pass `/update-engine` in a Desktop conversation rooted in `~/legacy-brain`** 🧑
- [ ] **B2 — F4 a full Desktop restart + resumed conversation picks up the new skill+MCP** 🧑
- [ ] **B3 — F2 real citation round-trip: 🔗 Notion opens, "open citation N" opens 🧠 in Obsidian, 🧠 inline with no Obsidian** 🧑
- [ ] **B4 — F8.1 Obsidian-absent install prints the opt-in recommendation; runtime hint is soft, not `broken`** 🧑
- [ ] **B5 — F8.2 real GUI register: Obsidian closed → vault appears; running / not-installed fail soft; first-launch caveat** 🧑
- [ ] **B6 — F5 the brain disambiguates the live Notion connector from the local mirror in conversation** 🧑
- [ ] **B7 — F7 a real OS toast fires the moment a capability newly breaks** 🤝
- [ ] **B8 — One clean full install on macOS (and, if possible, Windows) — the real-world cross-OS gate** 🧑
- [ ] **C1 — Confidential purge: `rm -rf ~/legacy-brain` after any pass that mirrored the real Notion zone; tarball untouched** 🤝
- [ ] **C2 — Final `/code-review` on the full branch (`main...HEAD`); fix findings in TDD if any** 🤝
- [ ] **C3 — Green light → Ship: merge + tag v3.3.0 + codename + archive (both plans + the findings backlog)** 🧑

---

## Part A — 🤖 What Claude runs now (deterministic, headless)

> Goal: take every check that does **not** require a human-visible GUI off Thomas's plate, with logged
> evidence (counts, exit codes, diffs) recorded against each item below.

### A1 — Suites + build green 🤖

- [ ] `node --test "scripts/*.test.mjs" "scripts/lib/*.test.mjs"` (harness — reconciler, update-engine,
  self-heal, health-check, obsidian-register, notify wiring) → record pass count.
- [ ] `cd rag && node --import tsx --test src/**/*.test.ts` → record pass count; then `npx tsc --noEmit`.
- [ ] `cd local-mirror && node --import tsx --test src/**/*.test.ts` → record pass count; then `npx tsc --noEmit`.
- [ ] `cd rag && npm run build` (esbuild bundle) succeeds — the shipped artifact, not just the sources.
- [ ] Record the final triple (expected ≈ harness 441 / rag 207 / local-mirror 87, all `tsc` clean — confirm
  the live numbers, do not trust the plan's snapshot).

### A2 — F1 headless upgrade replay (the centerpiece, mechanism-level) 🤖

The TRUE single-pass UX proof is human (B1). But the **mechanism** is fully checkable headless against the
pristine tarball. One disposable pass:

- [ ] `rm -rf /tmp/qa-legacy && mkdir -p /tmp/qa-legacy && tar -xzf ~/legacy-brain.tgz -C /tmp/qa-legacy`
  → a fresh v3.1.0 brain (rag 1.1.0) at `/tmp/qa-legacy/legacy-brain`. **Never extract onto `~/legacy-brain`
  for the headless pass** (that path is reserved for B1's real Desktop run); use `/tmp` so the two never collide.
- [ ] Snapshot the sacred surface BEFORE: hash `vault/`, `.env`, `CLAUDE.md`, `.claude/settings.json`, and the
  user-portion of `.mcp.json` (e.g. `find vault -type f -exec shasum {} + | sort` + `shasum` of the four files).
- [ ] Drive the converge headless: run the reconciler CLI against the extracted brain with `sourceDir` = this
  launcher (`node scripts/reconcile-brain.mjs --brainDir /tmp/qa-legacy/legacy-brain --sourceDir $PWD` — the
  same entry auto-finalize re-execs). Assert in **one** pass: the new engine skill dir(s) exist, `local-mirror`
  + `vault-rag` are present in the brain `.mcp.json`, launchers regenerated.
- [ ] **Idempotence:** run the same command a **second** time → assert zero churn (no files rewritten; `copied`
  empty; `.mcp.json` byte-identical).
- [ ] **F1 safety invariant:** re-hash the sacred surface AFTER both runs → assert `vault/` (minus the seeded
  `vault/engine-health/health-check.md`), `.env`, `CLAUDE.md`, `settings.json`, and any user-added `.mcp.json`
  server are **byte-identical** to the BEFORE snapshot. The ONLY allowed new vault file is the engine-health note.
- [ ] **F7-bis upgrader carve-out:** assert `vault/engine-health/health-check.md` is seeded on the upgrader
  (it was absent in the v3.1.0 tarball) and that a **second** reconcile does NOT re-seed / re-reindex it
  (`reindexReason` absent on run 2).
- [ ] `rm -rf /tmp/qa-legacy` when done.

> Honest scope of A2: this proves the **reconciler converges + is idempotent + leaves the vault sacred**. It
> does **not** prove the Node-module-cache "Layer A" auto-finalize child escape in a live `update-engine`
> process, nor the "config-freeze" capability pickup — those are B1/B2 (a running Desktop).

### A3 — F2 citations (renderer + headless search) 🤖

- [ ] Unit: `formatSearchCitations` test asserts, per citation, the affordance line
  `Ask me to "open citation N"…`, **both** emoji links (🧠 `obsidian://` + 🔗 `https`) and the copyable
  `vault/…` path are all present, numbering matches the heading; escaping tests (`<`/`>`/whitespace/parens) green.
- [ ] Headless end-to-end: against the launcher index, run a `search_vault` headlessly (or the health-check
  full read) and eyeball one real citation block carries the affordance + both links — confirm the renderer
  output shape on real data, not just a fixture.

### A4 — F7 / F7-bis / F7-ter health-check (headless, no 2nd server) 🤖

- [ ] `rag/src/health-check-cli.ts --depth light` against the launcher index → `{status:ok,…}`, **zero ONNX**,
  sub-second, no server boot.
- [ ] `--depth full` → real embed+search, canary **Quibblethorne** found (>0 hits), no server boot.
- [ ] `node scripts/verify-rag.mjs` from a brain context → headless full read, loud `exit 0` on the happy path;
  a deliberately-broken `vault-rag` `.mcp.json` entry → loud `exit 1` (mandatory broken). `local-mirror`
  unconfigured → `unknown`, benign, does NOT fail (gate carve-out, finding #3).
- [ ] `single-server-boot.test.mjs` green — the two sondes never import the MCP booter; only the installer does
  (regression guard against the `fc2e4bb` shape returning).
- [ ] Activation selector (`selectModulesToCheck`): mandatory `vault-rag` absent → `broken`; optional
  `local-mirror` absent → `skipped` silently; manifest-integrity guard locks `vault-rag = mandatory`.
- [ ] `formatHealthBanner`: all-ok → silent; core `vault-rag` broken → loud ⚠️ with the right gesture
  (reindex / embedder); optional `local-mirror` broken → soft ℹ️; **no** generic "run /update-engine".
- [ ] #5 fail-open: spawn `health-probe-run.mjs` against a **corrupt** `engine-manifest.json` → `exit 0`,
  no verdict written (the detached child never crashes).

### A5 — F8 Obsidian builders (pure + guarded I/O, headless) 🤖

- [ ] `addVaultToObsidianConfig`: idempotent (same path twice → one entry), **no-clobber** of existing vaults,
  deterministic 16-hex id, and **dedup by normalized `path`** (a vault already under Obsidian's OWN random id
  → no second entry — findings #7 + the case-insensitive macOS/Windows follow-up `33f87d4`).
- [ ] `registerVaultInObsidian` I/O wrapper: not-installed / running / already-registered / unreadable-JSON all
  → soft reason, **no write**; backup taken before any write; `SBG_NO_OBSIDIAN_REGISTER` / CI / headless opt-out honored.
- [ ] `obsidianHealth` + `formatObsidianHint`: present+registered → `ok`; absent or unregistered → `unknown`
  (soft nudge), **never `broken`**; corrupt `obsidian.json` → `unknown` (fail-soft). `runtimeObsidianHint`
  surfaces ONLY the actionable installed-but-unregistered case (anti-nag).
- [ ] `obsidianConfigPath`: darwin (`~/Library/Application Support`), win32 (`%APPDATA%`), linux
  (`~/.config` / `XDG_CONFIG_HOME`) branches all unit-covered.

### A6 — #8 notify escaping 🤖

- [ ] `buildNotifyCommand("darwin", { title, body: 'a "quote" `tick` $x' })` → the AppleScript arg escapes
  `"` / `` ` `` / `$` (no premature literal close); same for `win32` (PowerShell `` ` ``-escaping). `notify-send`
  (linux, array arg) left as-is. The health-check `detail` (incl. arbitrary spawn-error text) can no longer
  silently lose the toast.

### A7 — Cross-OS parity (win32 branches, from this Mac) 🤖

ADR 0015 gate. Confirm each `win32` branch is exercised by a unit test (a Mac-run `win32`-branch assertion
counts; record which test pins each):

- [ ] Self-heal / probe spawn shape (`detached` + `stdio:"ignore"` + `windowsHide:true` + `unref()`) — all four
  SessionStart hooks (`session-self-heal`, `session-health`, `session-obsidian-hint`, `session-status`).
- [ ] `notify.ts` detached notifier spawn carries `windowsHide` (the `1ddb282` gap fix) — assertion present.
- [ ] Self-copy guard (`copyInto`, `resolve(src)===resolve(dest)`) — locked OS-independently by reconcile test 6.
- [ ] `.mcp.json` server-id set + skill-dir existence use literal ids / POSIX globs `join`ed onto `brainDir`
  (no `\` vs `/` mismatch on win32).
- [ ] Citation opener allowlist carries `open` / `xdg-open` / `start`; `isObsidianRunning` carries pgrep +
  tasklist + assume-running fail-soft.

### A8 — Confidential hygiene 🤖 — DONE (2026-06-20)

- [x] `git grep` for the real ex-employer's tokens (brain name, Notion workspace slug, products, squads,
  UUIDs) across the repo → **0 hits.** The test fixtures that once carried a real workspace slug in their
  Notion URLs (`local-mirror/src/test/builder.ts` + four sibling `local-mirror/src/test/*.test.ts`) were
  scrubbed forward to the neutral `acme` slug, and the plan/doc mentions were neutralized too (commit
  `b5b673e`). Per memory `do-not-name-private-brain-in-launcher` the launcher must carry **zero** client
  tokens; this now holds repo-wide. _(Scrub forward only — no history rewrite.)_
- [x] `mollecuisse` occurrences (~35) are the **intentional** invented demo canary — left as-is (legit).
- [ ] Working tree clean: no stray QA driver / extracted brain / real `.env` committed (the `6ae98f6` /
  v3.2.1 lesson: `git add -A` once swept throwaway QA drivers in — re-check before any commit).

---

## Part B — 🧑 What Thomas runs by hand (real GUI / real services)

> These are the things I cannot observe: a running Claude Desktop's config-freeze, a real custom-scheme click,
> a real Obsidian switcher, a real OS toast, a second physical OS. Each uses the **QA harness loop**
> (`maintainers/plans/v3.3.0-…-action.md` § "QA harness"): re-extract `~/legacy-brain` from the pristine
> `.tgz` per pass, root a Desktop conversation in it, exercise, then purge if it mirrored the real zone.

### B1 — F1 real single-pass upgrade (the proof that matters) 🧑

- [ ] `rm -rf ~/legacy-brain && tar -xzf ~/legacy-brain.tgz -C ~` → fresh v3.1.0 brain.
- [ ] Open a Claude Desktop conversation **rooted in `~/legacy-brain`** (folder chip → pick the brain; `pwd`
  first message must show `~/legacy-brain`, NOT a temp dir).
- [ ] Run `/update-engine` **once**. Expect: the report names the newly-installed skill(s) + MCP, AND the loud
  **Layer-B activation notice** (N new capabilities, NOT active in THIS conversation yet, restart / "run once
  more" guidance) — finding #1 must NOT report the success as a failure even if auto-finalize hiccups.
- [ ] Confirm — in a SINGLE invocation — that the new `local-mirror` skill + MCP are installed on disk (no
  second `/update-engine` needed to put the files there). This is the auto-finalize Layer-A win.

### B2 — F4 restart picks up the new capability (not a brand-new conversation) 🧑

- [ ] After B1, **fully restart Claude Desktop** and **resume the same conversation** (do not start a new one).
- [ ] Confirm the new skill + MCP are now actually usable (e.g. the `local-mirror` skill triggers / the MCP
  tool answers) — proving "a full restart + resume" is the lighter sufficient action the brain now promises.
- [ ] Sanity: the hand-off copy did NOT tell the user to open a brand-new conversation just to pick up
  capabilities (that conflation was the F4 fix).

### B3 — F2 real citation round-trip 🧑

- [ ] Ask the brain a question whose answer cites a note (ideally a mirror note + a vault note).
- [ ] 🔗 **Notion source** click → opens the live page in the browser (https works in Desktop).
- [ ] Ask **"open citation N"** → Claude uses the allowlisted opener → the 🧠 local copy opens **in Obsidian**
  (with Obsidian installed + the vault registered from F8.2).
- [ ] **Zero-install fallback:** with Obsidian **absent**, confirm the 🧠 local copy still opens **inline in
  Claude Desktop** (side tab) — viewing the brain's own copy never *requires* Obsidian.
- [ ] Confirm the dead-click trap is gone: a bare 🧠 `obsidian://` click in Desktop does nothing, but the
  affordance text makes that a non-issue (the user knows to ask).

### B4 — F8.1 Obsidian-absent guidance 🧑

- [ ] On a machine / profile **without** Obsidian, run a fresh install → the installer prints the **opt-in
  recommendation** (free + open-source + viewer/editor over your own Markdown + obsidian.md + the one-time
  "Open folder as vault"). Install is **never blocked**.
- [ ] At runtime, the Obsidian hint is a **soft** optional nudge on its own channel — **not** the scary
  "broken" health banner — and does not nag every session.

### B5 — F8.2 real Obsidian register (GUI) 🧑

- [ ] Obsidian **installed + closed** → run install (or the opt-in question) → open Obsidian → the brain's
  vault appears in the switcher **already registered** (no manual "Open folder as vault").
- [ ] Re-run → **idempotent** (no duplicate entry — finding #7; also verify a vault the user had registered
  by hand is not duplicated).
- [ ] Obsidian **running** during install, and **not-installed** → both **fail soft** to the manual hint (no
  clobber of `obsidian.json`).
- [ ] **First-launch caveat:** on a brand-new Obsidian, exercise the vault-picker / welcome popup — confirm
  `obsidian://` opens can stall until the user picks a vault once, and the copy warns about it.

### B6 — F5 connector vs mirror disambiguation 🧑

- [ ] Ask an **ambiguous** Notion request ("can my brain search this Notion?") → the brain offers the balanced
  2-option choice (durable/offline → local mirror; one-off/live → native connector) and does not silently pick
  the wrong one.
- [ ] Ask a clearly **live/ad-hoc** Notion request → it uses the **native connector**, not `local-mirror`.

### B7 — F7 real OS toast on a newly-broken capability 🤝

- [ ] Induce a real break in a rooted brain (e.g. point the index at an empty/missing store, or remove embedder
  weights) → at the next session start the detached probe fires a **real OS notification** naming the broken
  capability — and a quote/backtick in the detail does NOT swallow the toast (#8). _(I can prepare the broken
  state headlessly; Thomas confirms the toast actually appears.)_
- [ ] A **still-broken** (not newly-broken) capability does NOT re-nag on every session.

### B8 — One clean full install, real OS (the cross-OS gate) 🧑

- [ ] One end-to-end fresh install on **macOS** following the bootstrap stub (in-process embedder path):
  installer self-verifies the Quibblethorne canary, post-flight `exit 0`, hand-off banner correct, brain
  answers from the vault in a freshly-rooted conversation.
- [ ] If a Windows machine is reachable: repeat the fresh install there (ADR 0015 parity — the unit-level
  win32 checks in A7 cover the branches, but one real Windows run is the gold standard). If not reachable this
  cycle, note it explicitly as a known coverage gap rather than implying it was tested.

---

## Part C — sign-off (on explicit green light)

- [ ] **C1 — Confidential purge** 🤝: after any pass that mirrored the **real** Notion zone, `rm -rf
  ~/legacy-brain`; confirm `~/legacy-brain.tgz` is **untouched** (still ~356 MB) and `git grep` finds 0
  references to the zone/workspace/token, and no client token (A8 scrubbed them all 2026-06-20).
- [ ] **C2 — Final `/code-review`** 🤝 on the full branch (`main...HEAD`, high effort). The fix-branch review
  is already done (`v3.3.0-code-review-fixes-action.md`); this is the whole-branch capstone. Any finding →
  fix in TDD baby-steps, green-only commit, then re-review.
- [ ] **C3 — Ship** 🧑 (Thomas's explicit green light only): merge + tag **v3.3.0** + GitHub release titled
  **"The One Where Upgrades Land In One Pass And Citations Open In Obsidian"**; then **archive** this QA plan,
  the main `v3.3.0-…-action.md`, the `v3.3.0-code-review-fixes-action.md`, and the findings backlog into
  `maintainers/plans/archived/` with STATUS ✅ + proof, and update the plans README (plan-done = archived).

---

## Evidence log (filled as items pass)

> One line per completed item: `(item) — result — date · evidence (counts / exit code / commit)`. This is the
> memory that survives a `/clear`, per the checkbox-plans convention.

- **A1** — PASS — 2026-06-20 — suites green: harness **441/441**, rag **207/207**, local-mirror **87/87**;
  `tsc --noEmit` exit 0 (rag + local-mirror); `npm run build` exit 0 both modules (build = `tsc` → `dist/`,
  **not** esbuild — the plan's "esbuild bundle" wording is stale; the shipped artifact still compiles clean).
  Live triple matches the snapshot (441/207/87).
- **A2** — PASS — 2026-06-20 — headless replay vs pristine `~/legacy-brain.tgz` (v3.1.0, rag 1.1.0, schema 1,
  `.mcp.json`=[vault-rag], no local-mirror skill) extracted to `/tmp/qa-legacy`. Drove the REAL `runReconcileCli`
  (the auto-finalize child path) with install/reindex seams stubbed. **Run 1:** `installedSkills=[local-mirror]`,
  `mcpServersAdded=[local-mirror]` (brain `.mcp.json` → [vault-rag, local-mirror]), launchers regenerated,
  health note seeded + `reindexReason="health-note-seed"` (incremental), local-mirror skill dir on disk. **Run 2
  idempotence:** no skill/MCP re-added, `.mcp.json` byte-identical. **Whole-tree content idempotence:** 279 files
  hashed, **0 differ** run→run. **Vault-sacred:** 0 vault files changed/removed, the ONLY new file is
  `vault/engine-health/health-check.md`; `.env` / `CLAUDE.md` / `settings.json` byte-identical. Launcher schema
  (1) == legacy schema (1) → a real upgrade does NOT full-reindex, only the health-note incremental pairing fires.
  _Nuances (intentional, not bugs):_ `copyInto` overwrites unconditionally → `copied`=211 every run (content
  byte-stable, vault untouched); `reindexReason` stays `health-note-seed` across runs by design (keyed off the
  note's on-disk presence, not a one-shot flag — so a crash-before-index run re-pairs). Honest scope: proves
  reconciler converge + idempotence + vault-sacred; does NOT prove the live-process Node-cache Layer-A escape (B1).
- **A3** — PASS — 2026-06-20 — `citation-renderer.test.ts` 5/5 (affordance numbered, 🧠+🔗 both links, `<…>`
  escaping for `>`/space/parens). Real render eyeballed: mirror note → 🧠 obsidian:// + 🔗 Notion + copyable
  `vault/…` path + `_Ask me to "open citation 1"…_`; vault note (no sourceUrl) → 🧠 only, numbering matches heading.
- **A4** — PASS — 2026-06-20 — `health-check-cli --depth light` → `{status:ok}`, **0.20s**, zero ONNX, no boot
  (index 33 rows, embedder gemini ready); `--depth full` → canary **Quibblethorne found (8 hits)**, 1.00s, no boot.
  `verify-rag.mjs` (synth `.mcp.json` from template, removed after): happy → **exit 0** (canary found, local-mirror
  `unknown`/optional skipped, benign); vault-rag removed → **exit 1** loud "not registered". Unit: single-server-boot
  + activation + health-probe + runner + manifest-integrity + headless-health-check + probe-run = **32/32**;
  rag health-check **16/16**. #5 fail-open: corrupt `engine-manifest.json` → `health-probe-run` **exit 0**, no verdict written.
- **A5** — PASS — 2026-06-20 — obsidian-register + obsidian-health + session-obsidian-hint = **38/38**. Named
  coverage: idempotent register, no-clobber, **deterministic 16-hex id** (`sha256(path).slice(0,16)`, empirically
  same path → same id), dedup by normalized path incl. **case-insensitive macOS/Windows** (#7 + `33f87d4`),
  I/O wrapper soft (config-absent / running→skip / already-registered no-write / malformed→fail-soft, backup
  before write), opt-outs (`SBG_NO_OBSIDIAN_REGISTER`/CI/headless), `obsidianHealth` never `broken`,
  `runtimeObsidianHint` anti-nag (installed-but-unregistered only), `obsidianConfigPath` darwin/win32/linux.
- **A6** — PASS — 2026-06-20 — `notify.test.ts` 25/25 incl. darwin (#8) + win32 (#8) escaping. Real eyeball of
  body `a "quote" \`tick\` $x`: darwin escapes `"`→`\"` in the AppleScript literal (backtick/`$` literal & safe as
  argv); win32 escapes `"`→`` `" ``, backtick→` `` `, `$`→`` `$ `` (PowerShell, no expansion); linux array arg left literal.
- **A7** — PASS (1 plan-wording nuance) — 2026-06-20 — full detached shape (`detached:true` + `stdio:"ignore"` +
  `windowsHide:true` + `.unref()`) in `session-self-heal`, `session-health`, `health-probe-run`, `notify.ts`;
  `headless-health-check` is `spawnSync` (synchronous) → `windowsHide` only (correct). _Nuance:_ the plan says "all
  four SessionStart hooks" but only **two** spawn a detached child (`session-self-heal`, `session-health`) — both
  hardened; `session-obsidian-hint` does no spawn (two file reads), `session-status` is a synchronous `git` call.
  Unit pins: `notify.test.ts` asserts `detached`+`windowsHide`; reconcile test 6 asserts the self-copy guard
  (`copied===[]`, OS-independent); opener allowlist `Bash(open|xdg-open|start:*)` in `settings.json.template`;
  `isObsidianRunning` pgrep+tasklist+assume-running unit-covered (A5).
- **A8** — PASS — 2026-06-20 — `git grep -il inqom` → **0** repo-wide (and 0 in the untracked working tree).
  Fixtures already neutralized to `acme` (`b5b673e`); the last residual mentions lived in THIS QA plan's A8 prose
  and were neutralized forward in this pass. `mollecuisse` (~35) left as-is (intentional canary). Working tree clean.
