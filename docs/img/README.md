# README screenshots

The README references two screenshots of the **Code** tab in Claude Desktop. Drop the two PNGs
here **with exactly these names**:

| Expected file | Screenshot content |
|---|---|
| `desktop-folder-chips.png` | The bottom of a **New session**: the row of chips `💻 Local · 📁 tmp · ➕`, just above the input field. |
| `desktop-recent-menu.png` | The **"Recent"** menu opened after clicking the folder chip: the list of recent folders with the **✓** on the current folder, and **"Open folder…"** at the bottom. |

Until these files are present, the two README images will appear broken.

## Notion-token guide screenshots

[`docs/notion-token-setup.md`](../notion-token-setup.md) uses seven screenshots of the **English**
Notion UI (`app.notion.com/developers/connections`). They live here **with exactly these names**:

| File | Screenshot content |
|---|---|
| `notion-token-01.png` | The **Developers → Connections** page (signed in), with **+ New connection**. |
| `notion-token-02.png` | The **New connection** dialog: name, **Access token** auth method, workspace, **Create connection**. |
| `notion-token-03.png` | The connection's **Configuration** tab: **Read content** ticked, the masked **Access token**. |
| `notion-token-04.png` | The **Content access** tab at **No page access**, with the **Edit access** button. |
| `notion-token-05.png` | The **Manage page access** dialog: searching for the root page. |
| `notion-token-06.png` | The **Manage page access** dialog: the root page ticked, **Save**. |
| `notion-token-07.png` | Back on **Configuration**: the **copy** icon on the Access token, **Copied** toast. |

Keep the filenames so the guide's image links don't break.
